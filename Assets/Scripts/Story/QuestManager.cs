using UnityEngine;

/// <summary>
/// Manages the state of all quests in the game. It tracks active quests,
/// checks for objective completion, and grants rewards.
/// </summary>
public class QuestManager : MonoBehaviour
{
    // Logic for starting, updating, and completing quests.
}